{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.zig$' && zig build 2>&1 || true; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Building..."
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.zig$' && zig build test 2>&1 || true; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Running zig tests..."
          }
        ]
      }
    ]
  }
}
